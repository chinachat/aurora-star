# Aurora Star 极光主题 — 使用文档

- 主题版本：2.0.1
- 作者：chinachat
- 项目地址：https://github.com/chinachat/aurora-star
- 适用 WordPress：6.0+ / PHP 7.4+
- 语言：简体中文（内置语言包）

---

## 目录

1. [安装与启用](#1-安装与启用)
2. [后台一级设置菜单](#2-后台一级设置菜单)
3. [主题设置（自定义器）](#3-主题设置自定义器)
4. [暗黑 / 明亮模式](#4-暗黑--明亮模式)
5. [导航菜单与菜单图标](#5-导航菜单与菜单图标)
5.5. [页脚结构与备案信息（ICP / 公安备案）](#55-页脚结构与备案信息icp--公安备案)
5.6. [友情链接](#56-友情链接)
6. [文章特色图](#6-文章特色图)
7. [文章浮动目录](#7-文章浮动目录)
8. [代码高亮](#8-代码高亮)
9. [图片灯箱（点击放大）](#9-图片灯箱点击放大)
9.5. [全局浮动导航按钮](#95-全局浮动导航按钮)
9.8. [评论增强（头像 / 归属地 / 系统与浏览器）](#98-评论增强头像--归属地--系统与浏览器)
9.9. [Markdown 写作](#99-markdown-写作)
10. [短代码完整示范](#10-短代码完整示范)
11. [常见问题](#11-常见问题)

---

## 1. 安装与启用

**方法一：后台上传（推荐）**

1. 在 WordPress 后台进入「外观 → 主题 → 添加新主题 → 上传主题」。
2. 选择 `aurora-star.zip`，点击「现在安装」。
3. 安装完成后点击「启用」。

**方法二：手动上传**

将 `aurora-star` 文件夹上传到服务器 `wp-content/themes/` 目录下，然后在后台「外观 → 主题」中启用。

> 提示：主题所有资源（图标库、代码高亮、JS/CSS）均已自托管，不依赖任何外部 CDN，可离线运行。

---

## 2. 后台一级设置菜单

启用主题后，后台侧边栏会出现一级菜单 **「Aurora Star 主题」**，点击进入可看到：

- 「打开主题自定义器」按钮：直达主题设置面板
- 各功能配置位置的说明速查表

所有可视化设置均在 **WordPress 自定义器** 中完成。

---

## 3. 主题设置（自定义器）

进入方式：`外观 → 自定义 → Aurora Star 主题设置`（或通过一级菜单一键直达）。

设置项分为以下几组：

### 3.1 颜色

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 主题主色 | `#6366f1` | 按钮、高亮、进度条等主色调 |
| 链接颜色 | `#4f46e5` | 正文链接颜色 |

### 3.2 暗黑模式

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 默认模式 | 跟随系统 | `跟随系统` / `明亮` / `暗黑` |
| 显示切换按钮 | 开启 | 页头月亮/太阳切换按钮 |
| 按时间段自动切换 | 开启 | 夜间 20:00–次日 6:00 自动启用暗色 |

### 3.3 文章目录

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 启用文章浮动目录 | 开启 | 文章右侧浮动目录 |
| 目录层级深度 | 3 | 2–6，数字越大包含标题层级越多 |
| 最少字数显示目录 | 300 | 字数不足时不显示目录 |

### 3.4 代码高亮

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 启用代码高亮（Prism） | 开启 | 基于 Prism 1.30 自托管 |
| 高亮主题 | Okaidia | 暗色主题 |
| 显示行号 | 开启 | 代码块左侧显示行号 |
| 代码自动换行 | 关闭 | 开启后长代码自动换行，不再横向滚动 |

### 3.5 图片灯箱

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 启用图片点击放大 | 开启 | 正文图片点击全屏预览 |
| 允许缩放与旋转 | 开启 | 滚轮/按钮缩放、拖动、旋转 |

### 3.6 文章

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 文章内显示特色图片 | 开启 | 关闭后单篇文章/页面顶部不显示特色大图（列表缩略图仍显示） |

### 3.7 背景

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 启用极光背景图 | 开启 | 浅色主题显示浅色极光背景，暗色主题自动切换暗色极光背景 |

### 3.8 页脚

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 页脚版权文案 | 空 | 不填则自动显示「© 年份 站点名」 |
| ICP 备案号 | 空 | 填入后页脚显示备案链接，**留空不显示** |
| ICP 备案链接 | `https://beian.miit.gov.cn/` | ICP 备案链接地址 |
| 公安备案号 | 空 | 填入后页脚显示公安备案（含盾牌图标），**留空不显示** |
| 公安备案链接 | 空 | 公安备案链接地址，留空则只显示文字不跳转 |

---

## 4. 暗黑 / 明亮模式

- 页头右侧的 **月亮/太阳按钮** 可随时切换，选择会被保存在浏览器中，下次访问自动生效。
- 若在设置中选择「跟随系统」，当系统切换到暗色主题时站点自动跟随。
- 开启「按时间段自动切换」后，夜间访问自动呈现暗黑模式。
- **极光背景**：浅色主题显示浅色极光背景图，暗色主题自动切换为暗色极光背景图（可在 `自定义器 → Aurora Star 主题设置 → 背景` 关闭）。

---

## 5. 导航菜单与菜单图标

**创建菜单**

1. 后台进入「外观 → 菜单」，新建或编辑菜单。
2. 在「显示位置」勾选「主导航」或「页脚导航」。
3. 将页面/文章/分类加入菜单并保存。

**设置菜单图标（需启用 Font Awesome 7 图标库）**

每个菜单项下方会出现「图标（Font Awesome 类名）」输入框，填入图标类名即可，例如：

| 示例 | 类名 |
|------|------|
| 首页 | `fa-solid fa-house` |
| 归档 | `fa-solid fa-folder` |
| 关于 | `fa-solid fa-user` |
| 搜索 | `fa-solid fa-magnifying-glass` |
| 联系 | `fa-solid fa-envelope` |
| GitHub | `fa-brands fa-github` |

图标会显示在菜单文字左侧，实心/描边/品牌图标均可。

> 类名可在 Font Awesome 官方图标库（fontawesome.com/search）查询，取 `fa-solid fa-xxx` 形式。

**支持多级下拉菜单**：在菜单中拖拽形成层级，桌面端鼠标悬停展开，移动端点击展开。

---

## 5.5 页脚结构与备案信息（ICP / 公安备案）

页脚自上而下分为若干行，**不再挤在一行**：

```
友情链接   友站 A   友站 B          ← 分配了「友情链接」菜单时显示
关于   联系   隐私政策               ← 页脚导航菜单

© 2026 站点名                        ← 版权
京ICP备00000000号 · 🛡 京公网安备…    ← 备案（单独一行）
由 Aurora Star 极光主题 驱动 · IP Geolocation by DB-IP   ← 署名
```

### 备案信息

在 `自定义器 → Aurora Star 主题设置 → 页脚` 中填写：

- **ICP 备案号**：填入后显示备案文字链接（如「京ICP备00000000号」），指向工信部备案查询系统。**留空则不显示**。
- **公安备案号**：填入后显示公安备案（带官方盾牌徽章，图标已本地化自托管），可填写公安备案查询链接。**留空则不显示**。

> 两者**单独占一行**，与版权文案分开；都留空则整行不输出。
> 两个链接都以新标签页打开，并带 `rel="noopener noreferrer nofollow"`。

### 主题署名

页脚「由 **Aurora Star 极光主题** 驱动」中的主题名是指向
[GitHub 仓库](https://github.com/chinachat/aurora-star) 的链接，新标签页打开。
可用过滤器改成自己的地址：

```php
add_filter( 'aurora_star_theme_url', function () {
    return 'https://your-site.com/';
} );
```

### 页脚链接都以新标签页打开

**页脚导航**与**友情链接**位置下的所有菜单链接都会自动加上
`target="_blank"` 与 `rel="noopener noreferrer"`；**主导航不受影响**。
如需关闭：

```php
add_filter( 'aurora_star_footer_links_new_tab', '__return_false' );
```

---

## 5.6 友情链接

主题预留了独立的「友情链接」菜单位置，不占用页脚导航：

1. 进入 **外观 → 菜单**，新建一个菜单（例如「友情链接」）
2. 添加自定义链接（友站名称 + 网址）
3. 在「显示位置」中勾选 **友情链接**，保存

菜单会显示在页脚顶部。支持主题的菜单图标功能——在菜单项的「图标」字段填
Font Awesome 类名（如 `fa-solid fa-link`）即可。

> 未分配菜单时该区域**完全不输出**，不会留下空白。

---

## 6. 文章特色图

- 在文章/页面编辑页右侧「特色图片」面板上传图片。
- **列表页**（首页/归档/搜索）：显示 640×360 缩略图。
- **文章详情页**：顶部显示 1600×800 大图。
- 可在 `自定义器 → Aurora Star 主题设置 → 文章` 中**关闭文章内特色大图显示**（列表缩略图不受影响）。
- 未设置特色图的文章，列表会显示占位图标。

---

## 7. 文章浮动目录

文章正文中的 `h2`、`h3`、`h4` 标题会自动生成目录，在桌面端右侧浮动显示：

- **滚动高亮**：随阅读位置高亮当前章节。
- **阅读进度条**：目录顶部显示文章阅读进度。
- **大类折叠**：含子标题的大类默认折叠，**读到该章节位置时自动展开其子类**；也可点击章节右侧箭头手动展开/折叠。
- **折叠/展开**：点击目录标题右侧箭头可收起。

**控制目录显示**

- 全局开关：`自定义器 → Aurora Star 主题设置 → 文章目录`。
- 单篇隐藏：编辑文章时，右侧「Aurora Star 文章选项」勾选「隐藏本文浮动目录」。
- 字数控制：文章字数少于设定值（默认 300）时不显示目录。

> 目录基于标题层级自动嵌套，标题顺序错乱时会按层级自动归类。

---

## 8. 代码高亮

基于自托管 **Prism.js 1.30**，支持 37 种常用语言，并带**行号（可开关）、复制按钮、语言标签**。

### 主题设置

在 `自定义器 → Aurora Star 主题设置 → 代码高亮` 中可配置：

| 设置 | 默认值 | 说明 |
|------|--------|------|
| 启用代码高亮（Prism） | 开启 | 全局代码高亮开关 |
| 高亮主题 | Okaidia | 暗色主题 |
| **显示行号** | 开启 | 代码块左侧显示行号 |
| **代码自动换行** | 关闭 | 开启后长代码自动换行，不再横向滚动 |

### 方式一：短代码（推荐）

```
[code lang="php"]echo "hello";[/code]
```

在后台编辑器（经典或区块编辑器）中直接输入上述短代码即可。参数说明：

| 参数 | 说明 |
|------|------|
| `lang` | 语言名称，如 `php`、`python`、`javascript`、`sql`、`json` 等 |
| `line` | `true` 强制显示行号，`false` 强制隐藏，不填（默认）跟随全局设置 |

**示例**：

```text
[code lang="php" line="true"]<?php echo "hi"; ?>[/code]

[code lang="python" line="false"]def hello():
    print("hi")[/code]
```

**注意**：区块编辑器（Gutenberg）会默认把内容中的换行和空格处理掉，建议代码内容用 HTML 实体写，或用经典编辑器，或直接在文本模式编辑。

**与 Markdown 发布器共存**：在 `wp-markdown-publisher` 的 `[markdown]` 区块（或 Markdown 文章）里用 `[code]` 时，
代码里的空行会被 Markdown 解析器当成段落分隔。主题 v1.5.4 起会自动把随之产生的孤立 `</p>` / `<p>`
还原成空行，因此下面两种写法都能得到正确结果：

```text
[code lang="php"]
a = 1

b = 2
[/code]
```

不过代码内容含 **HTML 标签**时更稳妥的写法是用围栏代码块，Markdown 发布器会原生处理，
不经过短码这一层：

````text
```html
<p>foo</p>
<p>bar</p>
```
````

### 方式二：直接写 HTML

在编辑器「文本」模式（经典）或「自定义 HTML」块中粘贴：

```html
<pre class="language-python"><code>def hello():
    print("hello world")</code></pre>
```

### 方式三：Gutenberg 代码块

直接使用区块编辑器自带的「代码」块，主题会自动检测代码语言并高亮（内置智能检测）。

### 支持的语言

`markup/html`、`css`、`clike`、`javascript`、`typescript`、`jsx`、`tsx`、`php`、`python`、`java`、`bash`、`json`、`sql`、`markdown`、`yaml`、`go`、`rust`、`c`、`cpp`、`csharp`、`ruby`、`swift`、`kotlin`、`docker`、`git`、`http`、`nginx`、`powershell`、`scss`、`diff`、`ini`、`properties`、`toml`、`vim`、`makefile` 等。

---

## 9. 图片灯箱（点击放大）

文章正文中的图片默认支持点击放大：

- 点击图片 → 全屏放大预览。
- **缩放**：鼠标滚轮 / 工具栏 `+ −` 按钮 / 键盘 `+` `-`。
- **拖动**：放大后按住拖动平移。
- **旋转**：工具栏旋转按钮或键盘 `R`。
- **重置**：工具栏按钮或键盘 `0`。
- **切换**：多图时使用 `←` `→` 方向键或工具栏左右按钮。
- **关闭**：点击遮罩、`×` 按钮或 `ESC`。
- **移动端**：支持双指捏合缩放、单指拖动。

如需关闭该功能：`自定义器 → Aurora Star 主题设置 → 图片灯箱`。

---

## 9.5 全局浮动导航按钮

页面右下角常驻一组浮动按钮：

- **返回顶部**：向下滚动超过 400px 后出现，点击平滑回到页顶。
- **分享本页**：点击弹出分享面板，支持：
  - 分享到 **微博**（新窗口打开分享页）
  - 分享到 **QQ**（新窗口打开分享页）
  - **复制链接**（一键复制当前页地址，含复制成功提示）
  - 微信入口提示（微信内建议使用右上角「···」转发）

> 浮动按钮全站生效，无需额外配置；样式随暗黑模式自动适配。

### 页头搜索

页头右侧有**搜索按钮**（放大镜图标），点击展开搜索面板：

- 输入关键词回车或点击「搜索」按钮提交
- 点击面板外区域或 `ESC` 关闭
- 移动端同样可用

---

## 9.8 评论增强（头像 / 归属地 / 系统与浏览器）

评论区每条评论在昵称后显示一行徽章：

```
[头像] 张三  [🇨🇳 中国 · 广东 · 深圳]  [🪟 Windows 10/11]  [🌐 Chrome 120.0.0.0]
       2026-09-12 12:00
```

### 头像（Gravatar）

评论头像使用 WordPress 内置的 `get_avatar()`，即 **Gravatar** 服务：

- 后台 **外观 → 自定义 → Aurora Star 主题设置 → 评论 → Gravatar 默认头像**
  可选择评论者没有 Gravatar 账号时显示的图案（神秘人 / 几何图形 / 像素风 / 机器人等）。
- 即使站点在「设置 → 讨论」里关闭了头像显示，主题仍会按评论邮箱请求 Gravatar，
  保证评论区不会出现空白头像；评论没有邮箱时退化为本地占位图。

### 操作系统与浏览器

- 数据来自评论自带的 User-Agent，**纯本地解析**，不产生任何外部请求。
- 可识别 Windows / macOS / iOS / iPadOS / Android / HarmonyOS / Linux / Ubuntu，
  以及 Chrome / Edge / Firefox / Safari / Opera / IE 和微信、QQ、UC、360、搜狗、百度等国产浏览器。
- 在 **自定义器 → 评论 → 显示操作系统与浏览器** 中可关闭。

> Windows 10 与 11 的 UA 完全相同，统一显示为「Windows 10/11」。

### 国旗与 IP 归属地

**需要自行准备离线 IP 库**，主题不附带。主题兼容**所有 MaxMind DB（mmdb）格式**的库，
按文件内容识别，不绑定厂商。

> ⚠️ **MaxMind GeoLite2 现对部分地区不再开放注册**。拿不到账号的话，直接用下面的
> **DB-IP Lite** 即可，效果基本一致。

#### 方式一：后台上传（推荐）

1. 从 <https://db-ip.com/db/lite.php> 直接下载（**无需注册**）：
   - 城市级：`dbip-city-lite-YYYY-MM.mmdb.gz`（gz ≈ 60 MB，可显示到城市）
   - 国家级：`dbip-country-lite-YYYY-MM.mmdb.gz`（**gz 仅 4 MB**，只有国家）
2. 进入 **后台 → Aurora Star 主题 → IP 归属地数据库**
3. 选择文件 → 点「上传并安装」

支持官方下载的 `.tar.gz`，也支持 `.gz` / `.zip` / 直接上传 `.mmdb`。
上传后会**先校验再替换**，校验失败会保留原有数据库，不会把功能弄坏。

面板会显示数据库类型、**构建时间**、节点数与文件大小——超过 45 天会提示该更新了。
点「删除后台上传的数据库」可一键回退。

#### 方式二：手动放置

放到 `wp-content/themes/aurora-star/assets/geoip/ip-database.mmdb`，
或放到主题目录之外（推荐），在子主题 `functions.php` 中指定：

```php
add_filter( 'aurora_star_geoip_db_path', function () {
    return WP_CONTENT_DIR . '/uploads/geoip/ip-database.mmdb';
} );
```

#### 查找优先级

`aurora_star_geoip_db_path` 过滤器 → 后台上传（`uploads/aurora-star-geoip/ip-database.mmdb`）
→ 兼容 v1.3.0 的旧文件名 → 主题目录。

#### 要点

- 没放数据库时该功能**静默关闭**，评论区不会报错，只是不显示归属地。
- 查询全部在**本地**完成，不会把访客 IP 发送给任何第三方，也不会显示 IP 本身。
- 结果按评论缓存，**每条评论只查一次**；已有评论会在首次被浏览时补上归属地。
  换库后旧评论不会自动重算，需要时删除 `_aurora_star_geo` 这个评论 meta 即可。
- **署名要求**：DB-IP Lite（CC BY 4.0）与 GeoLite2（CC BY-SA 4.0）都要求在展示其数据的
  页面保留署名。主题会自动在页脚输出对应来源的署名链接，可在
  **自定义器 → 评论 → 在页脚显示 IP 数据来源署名** 中关闭。
- 上传失败多半是 `php.ini` 的 `upload_max_filesize` / `post_max_size` 太小；
  换用体积只有 4 MB 的国家库通常就能绕过。
- 各数据源的对比与常见问题见 [`assets/geoip/README.md`](assets/geoip/README.md)。

---

## 9.9 Markdown 写作

主题**内置**了完整的 Markdown 发布功能（自 v2.0.0 起由独立插件 `wp-markdown-publisher`
并入，插件已退役，无需再安装）。

### 后台入口

后台左侧菜单 **Markdown 发布**，包含四个页面：

| 页面 | 用途 |
|------|------|
| 导入 Markdown | 拖放上传 `.md` / `.markdown` / `.txt`，也支持包含多个 Markdown 文件的 `.zip` |
| 批量重新渲染 | 解析器或渲染设置变化后，把已保存的原文重新渲染成最新格式的 HTML |
| 设置 | 文章类型、GFM 开关、目录层级、导入默认值、前台样式等 |
| 语法与说明 | 支持的语法与开发者接口 |

### 三种使用方式

**方式一：直接撰写（推荐）**

在文章编辑页，Markdown 编辑器会接管正文区，带工具栏与实时预览，写的是 Markdown，
保存时渲染成 HTML 存入正文。

**方式二：导入 `.md` 文件**

「导入 Markdown」页面上传文件即可批量生成文章。文件开头的 front matter 可设置
标题、slug、分类、标签、日期、摘要等：

```yaml
---
title: 我的文章
categories: [使用教程, WordPress]
tags: [Markdown, 插件]
---
```

**方式三：`[markdown]` 短代码**

在普通文章里用短代码包裹一段 Markdown：

```text
[markdown]
## 小标题

正文 **加粗**、`行内代码`。

| 列 A | 列 B |
| --- | --- |
| 1 | 2 |
[/markdown]
```

### 支持的语法

标题（ATX / Setext）、围栏与缩进代码块、引用、有序/无序/嵌套列表、任务列表、
表格、分隔线、链接与图片（行内 / 引用式）、脚注、行内 HTML、自动链接、
强调 / 加粗 / 删除线、代码段、目录（TOC）。

### 与短代码、代码高亮的关系

- `[markdown]` 区块**里**可以正常使用主题短码（`[tabs]` `[notice]` `[code]` 等），
  主题的短码保护在 Markdown 渲染之后运行，顺序是确定的。
- 代码围栏里的短码不会被当成短码执行（主题会把 `<pre>` 内的 `[` `]` 转义）。
- 在 `[markdown]` 区块里写 `[code]` 时，正文含**空行**也没问题；
  但代码内容是 HTML 标签时，更稳妥的写法是用围栏代码块：

  ````text
  ```html
  <p>foo</p>
  ```
  ````

### 从独立插件迁移

如果你是先在插件里写的内容，直接停用插件即可，**不需要做任何数据迁移**：

- 主题读取的是同一份数据（选项 `mdp_settings`，文章元数据 `_mdp_markdown` 等），
  已有的 Markdown 原文、渲染状态与全部设置原样继承。
- 插件仍在启用时，主题内置模块会自动让路并在后台给出提示；
  想回退到插件，重新启用插件即可——两边可以随时互换。
- 文章正文保存的始终是**渲染后的 HTML**，所以即使哪天不用本主题，文章内容也不会丢。

### 数据清理

主题没有"卸载"，对应的开关是 **Markdown 发布 → 设置 → 数据清理 →
「切换主题时同时删除设置项」**。勾选后，切走本主题时会删除 `mdp_settings`
与 `mdp_version` 两个选项；文章里的 Markdown 原文与已渲染的 HTML 不会被删除。

### 升级后建议重新渲染

标题锚点的生成规则在 **v2.0.1** 对齐了 GitHub（不再折叠连续的 `-`）。
**v2.0.1 之前**渲染过的文章，正文里已经写死的标题 id 还是旧格式，
手写的目录锚点可能点不动。

升级后到 **Markdown 发布 → 批量重新渲染** 跑一次即可；
编辑页也会提示「解析器已更新」，逐篇保存同样有效。

---

## 10. 短代码完整示范

以下是主题内置的全部短代码，可直接复制使用。

### 10.1 按钮 `[button]`

```text
[button href="https://example.com" color="primary" size="lg" target="_blank"]立即查看[/button]

[button href="/about" color="secondary" size="md" icon="fa-solid fa-info-circle"]了解更多[/button]

[button href="#" color="ghost" size="sm"]次要按钮[/button]

[button href="#" color="danger" size="md"]危险操作[/button]

[button href="#" color="success" size="md"]成功按钮[/button]
```

| 参数 | 可选值 | 默认值 | 说明 |
|------|--------|--------|------|
| `href` | URL | `#` | 链接地址 |
| `color` | `primary` `secondary` `ghost` `danger` `success` | `primary` | 颜色 |
| `size` | `sm` `md` `lg` | `md` | 尺寸 |
| `target` | `_blank` 等 | 空 | 新窗口打开 |
| `rel` | `nofollow` 等 | 空 | 链接关系 |
| `icon` | FA 类名 | 空 | 前置图标 |
| `class` | 自定义类 | 空 | 附加样式类 |

### 10.2 提示框 `[alert]`

```text
[alert type="info" title="提示"]这里是普通提示内容。[/alert]

[alert type="success" title="操作成功"]数据已保存。[/alert]

[alert type="warning" title="注意"]内容可能有变化，请留意。[/alert]

[alert type="error" title="错误"]发生了一个错误。[/alert]
```

| 参数 | 可选值 | 说明 |
|------|--------|------|
| `type` | `info` `success` `warning` `error` | 提示类型 |
| `title` | 任意文本 | 标题（可省略） |

### 10.3 注释块 `[note]`

```text
[note title="补充说明"]这是一段注释内容，常用于补充信息。[/note]

[note]无标题的注释块。[/note]
```

### 10.4 标签页 `[tabs]`

```text
[tabs]
[tab title="介绍"]这是第一个标签的内容。[/tab]
[tab title="用法"]这是第二个标签的内容。[/tab]
[tab title="示例"]这是第三个标签的内容。[/tab]
[/tabs]
```

| 参数 | 说明 |
|------|------|
| `active` | 默认激活的标签序号（从 1 开始），如 `active="2"` |

### 10.5 手风琴 `[accordion]`

```text
[accordion]
[accordion-item title="问题一" open="true"]第一个问题的答案。[/accordion-item]
[accordion-item title="问题二"]第二个问题的答案。[/accordion-item]
[accordion-item title="问题三"]第三个问题的答案。[/accordion-item]
[/accordion]
```

| 参数 | 说明 |
|------|------|
| `title` | 折叠项标题 |
| `open` | 设为 `true` 默认展开 |

### 10.6 代码 `[code]`

```text
[code lang="php"]<?php echo "Hello World"; ?>[/code]

[code lang="python" line="true"]def add(a, b):
    return a + b[/code]
```

参数见 [第 8 节](#8-代码高亮)。

**多行代码**：直接把代码写在短码标签之间即可，换行与空行都会原样保留：

```text
[code lang="php"]
function foo() {
    $a = 1;

    return $a;
}
[/code]
```

> 主题在 `wpautop` 之前对 `[code]` 内容做了保护，因此多行代码不会被 WordPress
> 自动插入 `<br />` 或 `<p>` —— 这类标记以前会被转义成字面量、显示在代码块里。

### 10.7 视频 `[youtube]`

```text
[youtube id="VIDEO_ID"]
```

| 参数 | 说明 |
|------|------|
| `id` | YouTube 视频 ID（URL 中 `v=` 后面的部分） |
| `width` | 宽度，默认自适应 |

### 10.8 图标 `[icon]`

```text
[icon name="fa-solid fa-star" size="2x" color="#f59e0b"]
```

| 参数 | 可选值 | 说明 |
|------|--------|------|
| `name` | FA 类名 | 图标，如 `fa-solid fa-heart` |
| `size` | `xs` `sm` `lg` `xl` `2xl` `1x`–`5x` | 尺寸 |
| `color` | 任意 CSS 颜色 | 颜色 |

### 10.9 免责声明 `[notice]`

```text
[notice]本文内容仅供学习参考，不构成任何建议。[/notice]
```

---

## 11. 常见问题

**Q：上传主题提示「主题缺少 style.css」？**

请确认下载的是最新版 `aurora-star.zip`（打包时已修正路径分隔符问题），并重新上传。

**Q：代码高亮没有生效？**

1. 确认在 `自定义器 → 代码高亮` 中已开启。
2. 确认代码块带语言标识：短代码 `[code lang="xxx"]` 或 `<pre class="language-xxx">`。
3. 刷新浏览器缓存（Ctrl+F5）。

**Q：文章目录没有显示？**

1. 确认文章字数达到「最少字数显示目录」阈值。
2. 确认文章包含 `h2`/`h3` 标题（正文标题，非文章标题）。
3. 检查文章编辑页右侧「隐藏本文浮动目录」未勾选。

**Q：菜单图标不显示？**

确认菜单项「图标」字段填写了完整的 Font Awesome 类名（如 `fa-solid fa-home`），且网站可访问主题内的 `fontawesome.min.css`。

**Q：暗黑模式按钮不工作？**

检查 `自定义器 → 暗黑模式 → 显示切换按钮` 是否开启；浏览器隐私模式下 localStorage 可能受限，但不影响切换。

**Q：如何更换代码高亮主题？**

目前内置 Okaidia（暗色）。如需其他 Prism 主题，可在主题目录 `assets/vendor/prism/themes/` 中加入对应 CSS 文件，并在 `inc/customizer.php` 的 `aurora_star_highlight_theme` 选项中添加。

---

© Aurora Star 极光主题 · by chinachat · 使用中遇到问题，欢迎反馈到 https://github.com/chinachat/aurora-star
